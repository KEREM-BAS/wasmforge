name: wasmforge
description: >-
  WASM-native building blocks for Flutter Web: typed JS interop, capability
  detection, and Web Worker + SharedArrayBuffer compute with graceful
  fallbacks.
version: 0.1.0
repository: https://github.com/KEREM-BAS/wasmforge
topics:
  - wasm
  - web
  - interop
  - workers
  - concurrency

environment:
  sdk: ^3.6.0

dependencies:
  web: ^1.1.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.0
